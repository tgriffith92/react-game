{
  "name": "project-template",
  "version": "1.0.0",
  "description": "# Setup",
  "main": "server.js",
  "dependencies": {
    "concurrently": "^4.1.1",
    "express": "^4.18.2",
    "matter-js": "^0.14.2",
    "mongoose": "^5.6.1",
    "react-game-engine": "^0.1.0",
    "react-game-kit": "^1.0.6"
  },
  "engines": {
    "node": "10.x"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"nodemon server.js\" \"cd client && npm start\"",
    "start": "node server.js",
    "postinstall": "cd client && npm i && npm run build"
  },
  "author": "",
  "license": "ISC"
}
